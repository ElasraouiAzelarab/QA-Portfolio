# pytest root configuration — applied to all test suites

[pytest]
markers =
    integration: marks tests that require network access to a live API
    selenium: marks tests that require a running browser (Chrome/ChromeDriver)
